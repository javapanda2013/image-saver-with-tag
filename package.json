{
  "name": "borgestag",
  "version": "1.46.20",
  "private": true,
  "description": "BorgesTag Firefox extension - dev tooling only (web-ext / ESLint)",
  "scripts": {
    "lint": "eslint --ext .js src/",
    "lint:fix": "eslint --ext .js --fix src/"
  },
  "devDependencies": {
    "eslint": "^9.39.4"
  }
}
